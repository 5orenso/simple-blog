'use strict';

var buster = require('buster'),
    assert = buster.assert,
    refute = buster.refute,
    article = require('../../lib/logger');

buster.testCase('lib/logger', {
    setUp: function () {
    },
    tearDown: function () {
    },
    'Test logger:': {
        'This is true': function () {
            assert(true);
        }





    }
});
